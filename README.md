Steps to Run
1. Run the docker desktop application on local machine.
2. Run the command "docker compose up -d" in the terminal.
3. Run the application from the main class.
4. Verify your application and docker are running.
5. Now go to the kafka-consumer application and run that application.
